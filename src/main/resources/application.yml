spring:
  application:
    name: bookrecommend
  datasource:
    url: jdbc:mysql://localhost:3306/book_recommend?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Ww2225747913.
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bookrecommend.entity
  configuration:
    map-underscore-to-camel-case: true # 开启驼峰命名转换

logging:
  level:
    com.bookrecommend.mapper: debug # 打印 SQL 日志

jwt:
  secret: your-very-secure-and-long-secret-key-at-least-256-bits-long
  expiration: 604800000 # 7天
