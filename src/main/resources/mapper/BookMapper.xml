<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookrecommend.mapper.BookMapper">

    <resultMap id="BookResultMap" type="com.bookrecommend.entity.Book">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="isbn" column="isbn"/>
        <result property="categoryId" column="category_id"/>
        <result property="description" column="description"/>
        <result property="cover" column="cover"/>
        <result property="publisher" column="publisher"/>
        <result property="publishDate" column="publish_date"/>
        <result property="tags" column="tags" typeHandler="com.bookrecommend.config.JacksonTypeHandler"/>
        <result property="content" column="content"/>
        <result property="stock" column="stock"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findList" resultMap="BookResultMap">
        SELECT * FROM book
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    title LIKE CONCAT('%', #{keyword}, '%') 
                    OR author LIKE CONCAT('%', #{keyword}, '%')
                    OR description LIKE CONCAT('%', #{keyword}, '%')
                    OR JSON_CONTAINS(tags, JSON_QUOTE(#{keyword}))
                )
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="tags != null and tags.size() > 0">
                AND
                <foreach collection="tags" item="tag" open="(" separator="AND" close=")">
                    JSON_CONTAINS(tags, JSON_QUOTE(#{tag}))
                </foreach>
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM book
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    title LIKE CONCAT('%', #{keyword}, '%') 
                    OR author LIKE CONCAT('%', #{keyword}, '%')
                    OR description LIKE CONCAT('%', #{keyword}, '%')
                    OR JSON_CONTAINS(tags, JSON_QUOTE(#{keyword}))
                )
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="tags != null and tags.size() > 0">
                AND
                <foreach collection="tags" item="tag" open="(" separator="AND" close=")">
                    JSON_CONTAINS(tags, JSON_QUOTE(#{tag}))
                </foreach>
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
